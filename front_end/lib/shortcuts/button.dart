import 'package:flutter/material.dart';

class button_shortcut extends StatefulWidget {
  const button_shortcut({super.key});

  @override
  State<button_shortcut> createState() => _button_shortcutState();
}

class _button_shortcutState extends State<button_shortcut> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
